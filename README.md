# draft_matchers

## Installation

Install gem
```rb
gem 'draft_matchers'
```
or
```
gem install draft_matchers
```

Update to `spec_helper.rb` or `rails_helper.rb`:
```
require "draft_matchers"
```

